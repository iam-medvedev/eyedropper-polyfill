export type Point = {
  x: number;
  y: number;
};

/**
 * Errors text
 */
export const errors = {
  canvasError: '[EyeDropper] Error getting canvas',
  magnifier: '[EyeDropper] Cannot create magnifier',
};
